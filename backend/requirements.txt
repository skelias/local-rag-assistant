# ===== 运行时依赖：别人 clone 项目后照着装的"购物清单" =====
# 写法两种：包名==精确版本（最稳） 或 包名>=最低版本（允许升）
# 铁律：每课装了新库，就同步在下面加一行 —— 清单永远等于事实！

# ---- 配置 ----
pydantic-settings>=2.5

# ---- P0-2 数据层 ----
aiosqlite>=0.20

# ---- P0-4 向量库与真实嵌入 ----
qdrant-client>=1.12          # Qdrant 本地文件模式（稠密+稀疏、RRF）
fastembed>=0.8               # 轻量真实嵌入(ONNX)：稠密 bge-small-zh + 稀疏 BM25

# 可选升级：BGE-M3（需 torch，体积大）。装上后把 embedding 实现换成 BGEM3Provider 即可
# FlagEmbedding>=1.2.11
# torch  # CPU 源安装：--index-url https://download.pytorch.org/whl/cpu

# ---- P0-3 模型层（云端 LLM） ----
anthropic>=0.40              # Claude 原生
openai>=1.55                 # OpenAI 兼容：DeepSeek / GPT / GLM / Kimi

# ---- P0-7 Web 层 ----
fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.1
python-multipart>=0.0.9
