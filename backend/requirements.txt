# ===== 运行时依赖：别人 clone 项目后照着装的"购物清单" =====
# 写法两种：包名==精确版本（最稳） 或 包名>=最低版本（允许升）
# 铁律：每课装了新库，就同步在下面加一行 —— 清单永远等于事实！

# 配置（读取 .env / 环境变量；pydantic 会自动一起装）
pydantic-settings>=2.5

# ------------------------------------------------------------------
# 以下库会在对应课程里逐个加进来（到时再取消注释/追加真实版本）：
# P0-2 数据层:   aiosqlite / qdrant-client
# P0-3 模型层:   anthropic / openai
# P0-4 向量层:   FlagEmbedding / torch（体积大，单独一课）
# P0-7 Web 层:   fastapi / uvicorn / sse-starlette / python-multipart / httpx
# ------------------------------------------------------------------
