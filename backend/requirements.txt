# ===== 运行时依赖：别人 clone 项目后照着装的"购物清单" =====
# 写法两种：包名==精确版本（最稳） 或 包名>=最低版本（允许升）
# 铁律：每课装了新库，就同步在下面加一行 —— 清单永远等于事实！

# 配置（读取 .env / 环境变量；pydantic 会自动一起装）
pydantic-settings>=2.5

# P0-2 数据层：异步访问 SQLite（连接我们的 data/rag.db）
aiosqlite>=0.20

# P0-4 向量库：Qdrant 本地文件模式（稠密+稀疏、RRF 融合检索）
qdrant-client>=1.12

# P0-3 模型层：云端 LLM 官方 SDK（DeepSeek 走 OpenAI 兼容接口）
anthropic>=0.40
openai>=1.55

# ------------------------------------------------------------------
# 以下库会在对应课程里逐个加进来（到时再取消注释/追加真实版本）：
# P0-4 真实嵌入:   FlagEmbedding / torch（体积大，单独一课；未装时测试用 FakeEmbedder 不依赖它）
# P0-6 文档解析:   （PDF/DOCX 解析器待选：Docling / unstructured）
# P0-7 Web 层:    fastapi / uvicorn / sse-starlette / python-multipart / httpx
# ------------------------------------------------------------------
